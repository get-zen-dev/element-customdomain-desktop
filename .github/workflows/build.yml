name: Simple Job On Different OS

on:
  workflow_dispatch:

jobs:
  write:
    strategy:
      matrix:
        os: [ubuntu-latest]
    
    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v3

    - timeout-minutes: 10
    - name: First Step
      run: echo "success"

    - name: Build
      run: yarn build-custom
